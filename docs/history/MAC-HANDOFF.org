#+title: Mac development handoff

Historical handoff from the initial prototype test pass. The branch and
manual publication instructions below are retained as history; current
release behavior is documented in README.org.

#+begin_example
Continue org-preview development on this Mac after its real Linux test pass.

Repository: https://github.com/chadhs/org-preview
Branch: MVP-v0.1
Linux implementation/testing commit: 687df3c

Use the existing Mac checkout and preserve unrelated local changes. Read
README.org, applicable AGENTS.md, and LINUX-TESTING.org first. Use the
checkout's direnv for the correct GitHub account. Fetch MVP-v0.1 and inspect
local/remote history before updating. Fast-forward only when safe; do not
reset local work or merge main.

Linux testing on Omarchy 4.0.2 / Hyprland 0.56.2 passed native Wayland and
XWayland development runs with Node 24.20.0. Native GTK picking and a real
Nautilus drag passed with spaces and Unicode. The AppImage worked with
--appimage-extract-and-run because FUSE 2 is absent; the extracted tar app
worked directly. File opening, live updates, atomic saves, deletion/recreation,
search-scroll preservation, and inert HTML/code passed in packaged apps.
Screenshots, logs and sandbox evidence are in docs/linux-testing/2026-09-08/.

The Linux changes fix mailto rendering, mixed ordered/unordered lists,
scroll jumps and lost active search matches after saves, and silently
ignored unsupported CLI paths. Smoke tests now require Chromium sandboxing
and cover these regressions, file-backed drops, errors and CLI handoff.
No dependencies or lockfile entries changed.

On this Mac, use Node 24 LTS and close running org-preview instances before
npm ci, npm run check, npm run test:smoke, and npm run dist:mac. Test the
actual development app and built .app from the packaged artifacts. Verify
native picking, Finder drag/drop and Open With, command-line/second-instance
opening, Unicode/spaced paths, search and shortcuts, themes, zoom, source
view, external saves, atomic replacement, deletion/recreation, file switching,
size/type errors, and inert HTML/source blocks. Verify actual default browser
and mail-client link opening; Linux smoke tests stubbed the OS opener.

Review Linux's remaining packaging warnings: default Electron icon and
unverified installed desktop-file association. Distinguish Mac defects from
unsigned-installer restrictions and v0.1 exclusions. Keep BSD-3-Clause and
the documented exclusions, retain the MIT Orga parser, and do not disable
Electron's sandbox. Do not add signing, auto-updates or excluded features
without a new scope decision.

Fix confirmed Mac problems, add focused regressions, rerun affected checks,
and record results in MACOS-TESTING.org. Commit and push to MVP-v0.1 without
AI author/co-author trailers. Do not merge main or publish a release.
#+end_example
